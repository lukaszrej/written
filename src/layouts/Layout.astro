---
import '~/assets/styles/tailwind.css';

import { I18N } from '~/utils/config';

import CommonMeta from '~/components/seo/CommonMeta.astro';
import Favicons from '~/components/seo/Favicons.astro';
import CustomStyles from '~/components/layout/CustomStyles.astro';
import ApplyColorMode from '~/components/seo/ApplyColorMode.astro';
import Metadata from '~/components/seo/Metadata.astro';
import SiteVerification from '~/components/seo/SiteVerification.astro';
import Analytics from '~/components/seo/Analytics.astro';
import BasicScripts from '~/components/seo/BasicScripts.astro';

// Comment the line below to disable View Transitions
import { ClientRouter } from 'astro:transitions';

import type { MetaData as MetaDataType } from '~/types';

export interface Props {
  metadata?: MetaDataType;
}

const { metadata = {} } = Astro.props;
const { language, textDirection } = I18N;
---

<!doctype html>
<html lang={language} dir={textDirection} class="2xl:text-[20px]">
  <head>
    <CommonMeta />
    <Favicons />
    <CustomStyles />
    <ApplyColorMode />
    <Metadata {...metadata} />
    <SiteVerification />
    <Analytics />

    <!-- Comment the line below to disable View Transitions -->
    <ClientRouter fallback="swap" />
  </head>

  <body class="antialiased text-default bg-page tracking-tight">
    <slot />

    <BasicScripts />
  </body>
</html>
